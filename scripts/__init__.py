"""
AI RemixMate — scripts package.

Exposes the top-level package so the project can be installed with
`pip install -e .` and all sub-packages (core, bridge) are importable
without any sys.path manipulation.
"""
