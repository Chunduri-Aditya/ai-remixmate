# =============================================================================
# AI RemixMate — Production Dependencies
# =============================================================================
# To install:  pip install -r requirements.txt
# After that:  pip install -e .   (installs the remixmate package itself)
# =============================================================================

# --- Audio core ---
librosa==0.11.0
soundfile==0.13.1
pydub==0.25.1
soxr==0.5.0.post1
audioread==3.0.1

# --- Stem separation ---
demucs==4.0.1

# --- Speech-to-text (lyrics extraction) ---
openai-whisper==20250625

# --- ML / embeddings ---
torch==2.7.1
torchaudio==2.7.1
scikit-learn==1.7.1
sentence-transformers==5.0.0
transformers==4.53.3
safetensors==0.5.3
huggingface-hub==0.33.4

# --- Numerical / signal processing ---
numpy==2.2.6
scipy==1.16.0
numba==0.61.2

# --- Data ---
pandas==2.3.1
pyarrow==21.0.0
PyYAML==6.0.2

# --- Download (see LEGAL NOTE below) ---
yt-dlp==2025.7.21
ytmusicapi==1.10.2          # official YouTube Music search API (anonymous, no key needed)
mutagen==1.47.0             # read/write audio metadata (ID3, MP4, etc.)

# --- Web / API (Phase 1 — backend) ---
fastapi==0.115.14
uvicorn[standard]==0.35.0
python-multipart==0.0.20    # file upload support for FastAPI

# --- Progress / CLI ---
tqdm==4.67.1
click==8.2.1

# --- Streamlit demo UI ---
streamlit==1.47.0
altair==5.5.0

# --- Loudness / mastering (AI Studio per-stem LUFS analysis) ---
pyloudnorm>=0.1.1              # ITU-R BS.1770-4 integrated loudness + true peak

# --- Neural audio codecs (optional — install separately if you want DAC tokenization) ---
# descript-audio-codec conflicts with streamlit's protobuf requirement and is
# imported with a try/except fallback — all core features work without it.
# To enable DAC tokenization:  pip install descript-audio-codec
# See pyproject.toml [project.optional-dependencies] generative group.

# --- VampNet (optional — inpainting via masked acoustic token modeling) ---
# VampNet is NOT on PyPI. Install from source when needed:
#   pip install git+https://github.com/hugofloresgarcia/vampnet
# The inpainting pipeline degrades gracefully to cosine-interpolation fallback
# if VampNet is not installed — all other AI Studio features work without it.

# --- Utilities ---
requests==2.32.4
tenacity==9.1.2
python-dateutil==2.9.0.post0
packaging==25.0
typing_extensions==4.14.1

# =============================================================================
# LEGAL NOTE — Downloading music
# =============================================================================
# yt-dlp is open-source (Unlicense).  However, downloading from YouTube or
# YouTube Music may violate those platforms' Terms of Service, especially for
# commercial use.
#
# For a production app serving users, prefer these fully-legal sources:
#
#   • Jamendo (https://www.jamendo.com)          — official API, CC licensed
#   • Free Music Archive (https://freemusicarchive.org) — CC licensed
#   • Musopen (https://musopen.org)              — public-domain classical
#   • Audionautix / Incompetech                  — CC BY
#
# ytmusicapi is used ONLY for search/metadata.  It does not download audio.
# yt-dlp downloading is appropriate for:
#   ✅  Personal use / local remix workflow
#   ✅  Portfolio / educational demos
#   ⚠️  Production SaaS — consult a lawyer; consider Jamendo/FMA instead
# =============================================================================
