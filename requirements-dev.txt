# =============================================================================
# AI RemixMate — Development / Testing Dependencies
# =============================================================================
# Install:  pip install -r requirements-dev.txt
# =============================================================================

-r requirements.txt          # include all production deps

# --- Testing ---
pytest==8.4.0
pytest-asyncio==0.25.3      # for testing async FastAPI routes

# --- Code quality ---
black==25.1.0               # formatter
ruff==0.12.0                # fast linter (replaces flake8 + isort)
mypy==1.16.0                # static type checker

# --- Pre-commit hooks ---
pre-commit==4.2.0
