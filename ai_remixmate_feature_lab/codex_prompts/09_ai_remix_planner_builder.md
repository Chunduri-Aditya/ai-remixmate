# AI Remix Planner Builder

You are working in the AI RemixMate repository, but your write scope is only `ai_remixmate_feature_lab/`.

Focus: Improve transition and remix recipe planning logic.

Rules:

- Read existing repository files only for context.
- Do not modify, rename, delete, reformat, stage, commit, branch, or auto-fix any file outside `ai_remixmate_feature_lab/`.
- Add or update tests for every new behavior.
- Keep base tests deterministic and free of network calls or heavy audio dependencies.
- Keep schemas, TypeScript types, and Python data shapes aligned when relevant.
- Run available tests from inside the lab package.
- Run no-touch verification from the repository root before final response.
- Write a final report under `ai_remixmate_feature_lab/reports/` describing changes, tests, risks, and any blockers.
